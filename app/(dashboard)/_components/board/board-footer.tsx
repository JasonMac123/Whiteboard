"use client";

interface BoardFooterProps {
  title: string;
  authorLabel: string;
  dateLabel: string;
}

export const BoardFooter = ({
  title,
  authorLabel,
  dateLabel,
}: BoardFooterProps) => {
  return <div></div>;
};
